package W8.D26F;

public class LinkedList {
    public static void main(String[] args) {
        // int r = (int)1e10;
        // int [] w = new int[r]; // throws error coz its demanding for continous memory
        // int q = (int)Math.pow(10, 10);
        // int [] e = new int[q]; // throws error coz its demanding for continous memory

        // arrays always use contigous memory 
        // due to this issue we use linked list which uses memory in parts 
        // LL uses nodes which store data of its own object and the other one next element
    }
}
